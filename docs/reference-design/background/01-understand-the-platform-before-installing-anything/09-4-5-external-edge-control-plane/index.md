# External edge control plane

Owned by:

```text
OpenTofu + provider APIs
```

Controls:

```text
Cloudflare DNS
Cloudflare Tunnel metadata
Cloudflare Access
relay VPS
relay firewall
public IP
```

---
