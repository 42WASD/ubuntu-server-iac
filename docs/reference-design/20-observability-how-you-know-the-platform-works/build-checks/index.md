# Build checks

Track `build01`:

```text
BuildKit cache size
free disk
build duration
cache hit effectiveness
concurrent builds
failed builds
CPU saturation
RAM pressure
builder GC events
Harbor push failures
```

---
