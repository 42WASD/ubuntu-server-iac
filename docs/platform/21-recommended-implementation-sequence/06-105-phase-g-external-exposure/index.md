# Phase G — external exposure

Build:

```text
Cloudflare
cloudflared
public HTTP route
Access for selected admin web apps
external status monitoring
```

Then:

```text
relay VPS
WireGuard
game port routing
```

---
