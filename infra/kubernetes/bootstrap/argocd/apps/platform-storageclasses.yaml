apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-storageclasses
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-20"
spec:
  project: platform

  source:
    repoURL: https://github.com/42WASD/ubuntu-server-iac.git
    targetRevision: main
    path: infra/kubernetes/platform/storageclasses

  destination:
    server: https://kubernetes.default.svc

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true