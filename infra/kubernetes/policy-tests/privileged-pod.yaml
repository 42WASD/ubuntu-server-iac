# Policy test fixture — INTENTIONALLY BAD (do NOT apply).
# Should be DENIED by disallow-privileged-host-settings / restricted PSA.
# Used only to prove the platform rejects privileged pods (Phase 30).
apiVersion: v1
kind: Pod
metadata:
  name: test-privileged
  namespace: dev-42wasd-admin
spec:
  containers:
    - name: nginx
      image: nginx:latest
      securityContext:
        privileged: true
        allowPrivilegeEscalation: true