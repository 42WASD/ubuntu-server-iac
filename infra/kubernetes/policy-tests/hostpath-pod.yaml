# Policy test fixture — INTENTIONALLY BAD (do NOT apply).
# Should be DENIED by disallow-privileged-host-settings (hostPath rule) /
# restricted PSA. Phase 30 fixture.
apiVersion: v1
kind: Pod
metadata:
  name: test-hostpath
  namespace: dev-42wasd-admin
spec:
  containers:
    - name: nginx
      image: nginx:latest
      volumeMounts:
        - name: host-root
          mountPath: /host
  volumes:
    - name: host-root
      hostPath:
        path: /