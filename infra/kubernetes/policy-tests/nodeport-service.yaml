# Policy test fixture — INTENTIONALLY BAD (do NOT apply).
# Should be DENIED by restrict-exposure-and-image-tags (NodePort rule).
# Phase 30 fixture.
apiVersion: v1
kind: Service
metadata:
  name: test-nodeport
  namespace: dev-42wasd-admin
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080